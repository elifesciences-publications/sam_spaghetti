# {# pkglts, conda
# requirements are managed by pkglts, do not edit this file at all
# edit .pkglts/pkg_cfg instead
# section pysetup
channels:
- defaults

dependencies:
# install
# run examples
# test
- coverage
- mock
- nose
# compile doc
- sphinx
# other dvlpt tools
# #}
